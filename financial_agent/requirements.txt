openai>=1.30
pymupdf>=1.24
pandas>=2.0
great-tables>=0.9
langchain>=0.2
langchain-text-splitters>=0.2
chromadb>=0.5
matplotlib>=3.8
streamlit>=1.35

# ── Production refactor 選用相依（只有開啟對應 FA_* 旗標才需要）──
# 混合檢索 FA_HYBRID_RETRIEVAL：
rank-bm25>=0.2          # BM25 稀疏檢索（缺少時 retrieval.py 用純 Python fallback）
tiktoken>=0.7           # 斷詞（缺少時用 regex fallback）
# 可選 cross-encoder rerank（FA_RERANK_MODEL 設定時）：
sentence-transformers>=2.2
# 互動式圖表 FA_PLOTLY：
plotly>=5.20
# LangGraph 編排 FA_USE_GRAPH：
langgraph>=0.2

# ── Teradata DB 查詢（database_query）相依 ──
pyodbc>=5.0             # 連 Teradata（另需安裝 Teradata ODBC 驅動）；開發機可不裝
tabulate>=0.9           # df.to_markdown() 前 30 筆預覽（缺少時退回 to_string）
# 前端高階數據組件（DB 大數據樹狀網格 + 自由樞紐；缺少時自動降級成一般表格）：
streamlit-aggrid>=1.0
streamlit-pivottablejs>=0.2
